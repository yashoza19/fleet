# Extra vCluster Helm overrides for manual testing.
# The core values (SAN, RBAC) are generated by fleet-create-test-vcluster
# from --cluster-name and --namespace. This file is for additional overrides
# passed via --values-file.
sync:
  toHost:
    ingresses:
      enabled: true
