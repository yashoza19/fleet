apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: verify-tier-workloads
spec:
  params:
    - name: cluster-name
      type: string
    - name: pipeline-image
      type: string
    - name: tier
      type: string
  workspaces:
    - name: source
  steps:
    - name: verify
      image: $(params.pipeline-image)
      imagePullPolicy: IfNotPresent
      script: |
        #!/usr/bin/env bash
        set -euo pipefail
        fleet-verify-tier-workloads \
          --cluster-name "$(params.cluster-name)" \
          --tier "$(params.tier)" \
          --spoke-kubeconfig "$(workspaces.source.path)/kubeconfig"
      when:
        - input: "$(params.tier)"
          operator: notin
          values: ["base"]