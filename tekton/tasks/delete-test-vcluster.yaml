apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: delete-test-vcluster
spec:
  params:
    - name: cluster-name
      type: string
    - name: namespace
      type: string
    - name: pipeline-image
      type: string
  workspaces:
    - name: shared
  steps:
    - name: delete
      image: $(params.pipeline-image)
      imagePullPolicy: Always
      script: |
        #!/usr/bin/env bash
        set -euo pipefail
        fleet-delete-test-vcluster \
          --cluster-name "$(params.cluster-name)" \
          --namespace "$(params.namespace)"
