apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# This kustomization combines both subscription and activation phases
# The apply_tier_workloads.py tool will use the subdirectories separately
# but this allows for complete virt workload builds when needed

resources:
  - subscription
  - activation